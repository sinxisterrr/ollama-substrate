# Substrate AI Framework - Dependencies
# Install with: pip install -r requirements.txt

# ============================================
# CORE FRAMEWORK (Required)
# ============================================
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SocketIO==5.3.5      # WebSocket support for streaming
python-socketio==5.10.0    # SocketIO core
eventlet==0.36.1           # Async server for Flask-SocketIO
gevent==24.2.1             # Alternative async (fallback)

# ============================================
# API CLIENTS (Required)
# ============================================
openai==1.54.3              # OpenAI-compatible client for OpenRouter
aiohttp==3.9.1              # Async HTTP for streaming
httpx>=0.25.0               # Modern HTTP client (async support)
requests==2.31.0            # Sync HTTP for simple calls

# ============================================
# MEMORY & STORAGE (Required)
# ============================================
chromadb==0.4.18            # Vector database for archival memory

# Ollama Integration (for local embeddings)
ollama==0.1.7               # Official Ollama Python client

# ============================================
# DATA PROCESSING (Required)
# ============================================
pydantic==2.5.0             # Data validation
python-dotenv==1.0.0        # Environment variable management
demjson3==3.0.6             # Robust JSON parsing
tiktoken==0.5.2             # Token counting for context window

# ============================================
# SYSTEM UTILITIES (Required)
# ============================================
psutil>=5.9.0               # System monitoring (health checks)

# ============================================
# DATABASE (Optional but Recommended)
# ============================================
psycopg2-binary==2.9.9      # PostgreSQL support
neo4j==5.14.1               # Neo4j Graph DB (for Graph RAG)

# ============================================
# MCP INTEGRATION (Optional)
# ============================================
# mcp==0.1.0                # MCP client for tool servers

# ============================================
# TOOL INTEGRATIONS (Optional)
# ============================================
# Uncomment if you want to use these features:
# discord.py==2.3.2         # For Discord tool
# spotipy==2.23.0           # For Spotify control

# ============================================
# DEVELOPMENT (Optional)
# ============================================
pytest==7.4.3               # Testing
pytest-asyncio==0.21.1      # Async testing

# ============================================
# LOGGING & MONITORING
# ============================================
colorama==0.4.6             # Colored terminal output
python-json-logger==2.0.7   # Structured logging

# ============================================
# PRODUCTION (Optional)
# ============================================
gunicorn==21.2.0            # WSGI HTTP Server

